# 工作知识库

记录工作中遇到的问题和注意事项，方便日后查阅。

---

## 1. 埋点需求处理规范

做需求时，如果遇到需要埋点的需求，注意以下事项：

- 埋点字段**必须**和《PRD6-量化王的埋点规范》里面的字段保持一致。
- 如果现有字段无法满足埋点需求，**必须单独提一个埋点需求**给到臧经理（臧云飞）。

---

## 2. 验证码接口

验证码可以使用以下接口：

- 接口地址：`http://192.168.11.231:3000/project/862/interface/api/635372`

---

## 3. 策略回测数据获取逻辑

- 必须有人**主动发起一条回测请求**，才会生成新的回测 ID，并重新进行回测以获取最新数据。
- 如果没有人主动发起新的回测请求，则回测 ID 保持不变。
- 根据**相同的回测 ID** 获取到的数据**始终不变**。

---

## 4. 提 UI 需求前确认首屏内容

每次提 UI 需求之前，**必须和需求方确认**进入页面首屏时需要展示哪些内容。否则 UI 按照自身设计规范，可能会将部分元素做得较大，导致首屏无法展示关键信息。
